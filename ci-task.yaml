acceptance_criteria:
- GET /burning-ship returns Burning Ship fractal data as JSON, same response shape
  as /fractal
- 'accepts query params: iterations, width, height, xmin, xmax, ymin, ymax'
- iterations clamped 1-200, width clamped 100-800, height clamped 75-600
- all existing tests still pass
- ruff lint reports zero errors
- no critical or high security findings
base_branch: main
description: '## Acceptance Criteria

  - GET /burning-ship returns Burning Ship fractal data as JSON, same response shape
  as /fractal

  - accepts query params: iterations, width, height, xmin, xmax, ymin, ymax

  - iterations clamped 1-200, width clamped 100-800, height clamped 75-600

  - all existing tests still pass

  - ruff lint reports zero errors

  - no critical or high security findings


  ## Agents

  - repo_inspector

  - coding

  - test_pyramid

  - security

  - lint

  - code_quality

  - docker'
execution_id: trello-nYkVhmFI
metadata:
  trello_board_id: 6a784e64550433b462147902
  trello_card_id: 6a7b66d5a76aca120d4d8ea3
  trello_list_id: 6a7b616b353151562a87d6a7
  trello_short_link: nYkVhmFI
  trello_url: https://trello.com/c/nYkVhmFI/9-review-add-burning-ship-fractal-endpoint
mode: pr
repository_path: examples/sample-service
requested_agents:
- repo_inspector
- coding
- test_pyramid
- security
- lint
- code_quality
- docker
source: trello
title: '[REVIEW] Add Burning Ship fractal endpoint'
